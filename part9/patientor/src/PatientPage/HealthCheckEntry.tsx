import React from 'react';
import { Entry } from '../types';

const HealthCheckEntry: React.FC<{ entry: Entry }> = ({ entry }) => {
    return <div>HEALTH CHECK ENTRY</div>;
};

export default HealthCheckEntry;
